---
paths:
  - ".github/**"
---

# CI (GitHub Actions)

- **Pin every third-party action to a full commit SHA, never a tag or branch** (e.g. `actions/checkout@<sha>  # v4.2.2`). Tags are mutable and a supply-chain risk; keep the human-readable version in a comment
